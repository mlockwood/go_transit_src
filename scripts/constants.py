from src.scripts.utils.functions import find_path


PATH = find_path('go')

DATA_PATH = '{}/data'. format(PATH)

REPORT_PATH = '{}/report'. format(PATH)

SRC_PATH = '{}/src'. format(PATH)
APP_PATH = '{}/src/apps'.format(PATH)
SCRIPT_PATH = '{}/src/scripts'. format(PATH)
